---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: radarr-requests
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: scw-sm
  target:
    name: radarr-requests
  refreshInterval: 60s
  dataFrom:
    - extract:
        key: path:/arr-apps/radarr-requests
        conversionStrategy: Default
        decodingStrategy: None
        metadataPolicy: None
