---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "base-app.name" . }}-config
data:
  config: |
{{ .Files.Get "assets/config.yml" | indent 4 }}
  page-home: |
{{ .Files.Get "assets/pages/home/page.yml" | indent 4 }}
  page-youtube: |
{{ .Files.Get "assets/pages/youtube/page.yml" | indent 4 }}
