# Default values for base-app.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

# This is to override the chart name.
nameOverride: ""
fullnameOverride: ""

# This will set the replicaset count more information can be found here: https://kubernetes.io/docs/concepts/workloads/controllers/replicaset/
replicaCount: 1

updateStrategy:
  type: Recreate

# This sets the container image more information can be found here: https://kubernetes.io/docs/concepts/containers/images/
image:
  # This sets the pull policy for images.
  pullPolicy: IfNotPresent
  repository: ""
  # Overrides the image tag whose default is the chart appVersion.
  tag: ""

# This is for setting Kubernetes Annotations to a Pod.
# For more information checkout: https://kubernetes.io/docs/concepts/overview/working-with-objects/annotations/
podAnnotations: {}
# This is for setting Kubernetes Labels to a Pod.
# For more information checkout: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
podLabels: {}

# -- (map) securityContext applied to all pods
podSecurityContext:
  # -- (bool) Indicates that the container must run as a non-root user.
  runAsNonRoot: true
  # -- (int) The UID to run the entrypoint of the container process.
  runAsUser: 10001
  # -- (int) The GID to run the entrypoint of the container process.
  runAsGroup: 10001
  # -- (int) A special supplemental group that applies to all containers in a Pod.
  fsGroup: 10001
  # -- (map) The seccomp options to use by the containers in this pod.
  seccompProfile:
    # -- (string) The type of seccomp profile (e.g. Localhost, RuntimeDefault, Unconfined).
    type: RuntimeDefault

# -- (map) securityContext applied to all containers
containerSecurityContext:
  # -- (bool) Indicates that the container must run as a non-root user.
  runAsNonRoot: true
  # -- (bool) Mounts the container's root filesystem as read-only.
  readOnlyRootFilesystem: true
  # -- (int) The UID to run the entrypoint of the container process.
  runAsUser: 1000
  # -- (bool) Run the container in privileged mode.
  privileged: false
  # -- (bool) Controls whether a process can gain more privileges than its parent process.
  allowPrivilegeEscalation: false
  # -- (map) The capabilities to add/drop when running containers.
  capabilities:
    # -- (list) List of capabilities to drop.
    drop:
      - ALL
  # -- (map) The seccomp options to use by the containers in this pod.
  seccompProfile:
    # -- (string) The type of seccomp profile.
    type: RuntimeDefault

# This is for setting up a service more information can be found here: https://kubernetes.io/docs/concepts/services-networking/service/
service:
  enabled: true
  # This sets the service type more information can be found here: https://kubernetes.io/docs/concepts/services-networking/service/#publishing-services-service-types
  type: ClusterIP
  # This sets the ports more information can be found here: https://kubernetes.io/docs/concepts/services-networking/service/#field-spec-ports
  port: 80
  scheme: http
  annotations: {}

# This block is for setting up the ingress route for more information can be found here: https://kubernetes.io/docs/concepts/services-networking/ingress/
ingressRoute:
  enabled: false
  annotations: {}
  # matchRule: Host(`test.example.com`)
  middlewares: []
    # - name: middleware1
    #   namespace: apps

resources: {}
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  # limits:
  #   cpu: 100m
  #   memory: 128Mi
  # requests:
  #   cpu: 100m
  #   memory: 128Mi

env: []
args: []
command: []

# This is to setup the liveness and readiness probes more information can be found here: https://kubernetes.io/docs/tasks/configure-pod-container/configure-liveness-readiness-startup-probes/
livenessProbe: {}
readinessProbe: {}

# Additional volumes on the output Deployment definition.
volumes: []
  # - name: foo
  #   secret:
  #     secretName: mysecret
  #     optional: false

# Additional volumeMounts on the output Deployment definition.
volumeMounts: []
  # - name: foo
  #   mountPath: "/etc/foo"
  #   readOnly: true

secrets:
  env: {}

pvc:
  enabled: false
  name: "data"
  mountPath: ""
  size: ""
  accessModes:
    - ReadWriteOnce
  cloneFromNfsPath: null